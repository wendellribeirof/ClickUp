import loginPage from "../support/loginPage";
import {el} from "../support/elementsLogin";
describe("Acesso ao login", () => {

    beforeEach(() => {
        loginPage.visit();
    });

    context('', () => {
        it('', () => {
            
        });
    });
});


